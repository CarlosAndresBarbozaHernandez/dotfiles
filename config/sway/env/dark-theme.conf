# --------------------------------------------------
# Variables de entorno para tema oscuro
# Modo oscuro en GTK, Qt y aplicaciones Wayland
# --------------------------------------------------

# GTK (NO usar "setenv", solo comentarios informativos)
# Se configura con gsettings en system.conf

# Qt - Estas se exportan en ~/.profile o script de inicio
# QT_QPA_PLATFORMTHEME=qt5ct
# QT_STYLE_OVERRIDE=kvantum

# Variables de sesión para Wayland
set $xdg_current_desktop sway
set $xdg_session_desktop sway
set $xdg_session_type wayland
